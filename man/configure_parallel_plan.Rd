% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{configure_parallel_plan}
\alias{configure_parallel_plan}
\title{Configure Intelligent Parallel Plan with Load Balancing}
\usage{
configure_parallel_plan(
  loop_number,
  strategy = "multicore",
  n_workers = NULL,
  reserve_cpu = 0.2,
  ...
)
}
\arguments{
\item{loop_number}{integer. The total number of iterations or tasks to be executed (e.g., bootstrap samples \code{B} or repeated holdouts \code{rh}).
需要并行执行的任务/循环总数。}

\item{strategy}{character. The parallel strategy to use. Defaults to "multicore" (efficient for Linux/macOS).
\strong{Note}: Windows users should strictly use "multisession".
并行策略，默认为 "multicore"。Windows 用户请务必使用 "multisession"。}

\item{n_workers}{integer. Optional. Manually specify the number of workers.
If NULL (default), the function calculates the optimal number automatically.
手动指定核心数。若为 NULL 则触发自动优化算法。}

\item{reserve_cpu}{numeric. The proportion of system CPU to reserve (0 to 1). Default is 0.2 (20\%).
保留给系统的 CPU 比例，防止死机。默认为 0.2。}

\item{...}{Additional arguments passed to \code{future::plan()}.}
}
\value{
The \emph{previous} future plan (invisibly). This is intended to be used with \code{on.exit()} to restore the environment state after execution.
返回旧的并行计划（不可见对象），必须配合 \code{on.exit()} 使用以在函数结束时还原环境。
}
\description{
Automatically configures the parallel processing plan using the \code{future} package.
It implements a "Smart Load Balancing" strategy to optimize resource usage based on
the total number of tasks (\code{loop_number}) and available CPU cores.
\cr
该函数利用 \code{future} 包自动配置并行处理计划。它采用“智能负载均衡”策略，根据任务总数和可用 CPU 核心优化资源使用。
}
\details{
This function performs the following steps to ensure efficient and safe parallelization:
\enumerate{
\item \strong{Check Existing Plan}: If a parallel plan is already active (set externally), it respects the current settings and returns immediately to avoid conflicts.
\item \strong{Sequential Mode}: If \code{strategy} is "sequential" or \code{loop_number} <= 1, it sets a sequential plan.
\item \strong{Smart Load Balancing}: If \code{n_workers} is NULL, it calculates the optimal number of workers:
\itemize{
\item \strong{Safety}: Limits usage to \code{(1 - reserve_cpu)} of total cores to prevent system freeze.
\item \strong{Efficiency}: Calculates the minimum number of batches required to finish all loops.
\item \strong{Balance}: Distributes workers evenly across batches to minimize memory spikes and CPU contention.
\item \emph{Example}: Running 10 tasks on a machine with 8 safe cores. Instead of running 8 workers then 2 workers (unbalanced), it runs 2 rounds of 5 workers.
}
}
}
