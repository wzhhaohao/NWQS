% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{calc_spline_wqs_weights}
\alias{calc_spline_wqs_weights}
\title{Calculate Spline-WQS Weights (Single Iteration)
计算 Spline-WQS 权重 (单次迭代)}
\usage{
calc_spline_wqs_weights(
  data,
  mix_name,
  dependent_var = "y",
  expand_func = wqs_nonlinear_expand,
  shuffle = 100,
  ...
)
}
\arguments{
\item{data}{data.frame. The full dataset containing mixture and covariates.
包含混合物和协变量的完整数据框。}

\item{mix_name}{character vector. Names of the mixture components (original variable names before expansion).
混合物组分的变量名向量（展开前的原始变量名）。}

\item{dependent_var}{character. Name of the dependent variable (outcome). Defaults to "y".
因变量名称。默认为 "y"。}

\item{expand_func}{function. Function used to non-linearly expand the mixture variables.
Defaults to \code{wqs_nonlinear_expand}.
用于非线性展开混合变量的函数。默认为 \code{wqs_nonlinear_expand}。}

\item{shuffle}{integer. Number of permutations for calculating variable importance.
Higher values provide more stable importance scores but increase computation time. Default is 100.
计算变量重要性时的置换次数。数值越高评分越稳定，但计算时间增加。默认为 100。}

\item{...}{Additional arguments passed to \code{expand_func}.
Critical arguments include:
\itemize{
\item \code{df_spline}: Degrees of freedom for the spline expansion (default 3).
\item \code{transform_fun}: Quantile transformation function.
}
传递给 \code{expand_func} 的额外参数。}
}
\value{
numeric vector. A named vector of normalized weights for each mixture component.
Returns \code{NA} if all importance scores are zero or negative.
Returns \code{NULL} if OOB sample size is zero (edge case).
返回每个混合物组分的归一化权重向量。如果重要性均为负或0返回 NA。
}
\description{
Performs a single iteration of the WQS regression process using spline expansion.
The workflow includes:
\enumerate{
\item Bootstrapping the dataset (separating In-Bag and Out-of-Bag samples).
\item Non-linearly expanding mixture variables using splines.
\item Fitting a Generalized Linear Model (GLM) on the training set.
\item Calculating variable importance via \strong{Grouped Permutation} on the OOB set.
\cr
执行单次 Spline-WQS 迭代。工作流包括：
\item 数据集重抽样（分为袋内和袋外数据）。
\item 使用样条函数对混合变量进行非线性展开。
\item 在训练集上拟合广义线性模型 (GLM)。
\item 在 OOB 数据集上通过 \strong{成组置换 (Grouped Permutation)} 计算变量重要性。
}
}
\details{
\strong{Grouped Permutation Importance (成组置换重要性):}
Unlike standard permutation importance where columns are shuffled independently, this function
respects the structure of spline basis functions. When calculating the importance of a mixture component
(e.g., "Chemical_A"), all its corresponding spline basis columns (e.g., "Chemical_A_basis1", "Chemical_A_basis2")
are shuffled \strong{synchronously} using the same random index. This preserves the internal correlation structure
of the spline expansion while breaking the relationship with the outcome.
\cr
与标准置换重要性不同，本函数尊重样条基函数的结构。在计算某个混合组分（如 "Chemical_A"）的重要性时，
它对应的所有样条基函数列（如 "Chemical_A_basis1", "Chemical_A_basis2"）会使用相同的随机索引\strong{同步洗牌}。
这样既打破了与因变量的联系，又保留了样条展开内部的相关结构。

\strong{Weight Calculation:}
\deqn{w_i = \frac{\sqrt{Imp_i}}{\sum \sqrt{Imp}}}
Where \eqn{Imp_i} is the increase in MSE when variable \eqn{i} is shuffled.
}
