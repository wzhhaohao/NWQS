% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-predict.R
\name{predict.nwqs}
\alias{predict.nwqs}
\title{Predict Method for NWQS Models}
\usage{
\method{predict}{nwqs}(
  object,
  newdata = NULL,
  type = c("response", "link", "nwqs_index"),
  ...
)
}
\arguments{
\item{object}{An object of class \code{"nwqs"}.}

\item{newdata}{Optional \code{data.frame}. If \code{NULL}, the training
data stored on \code{object$data} is used.}

\item{type}{Character. One of \code{"response"} (default),
\code{"link"}, or \code{"nwqs_index"}.
\itemize{
\item \code{"nwqs_index"} returns the 1-DoF latent NWQS score for
each row of newdata.
\item \code{"link"} returns the full linear predictor
\eqn{X\beta} including the intercept, the \code{nwqs} term, and any
covariates that were in the original fit.
\item \code{"response"} returns the inverse-link transform of the
linear predictor (identity for Gaussian, \code{plogis} for binomial,
\code{exp} for Poisson / quasi-Poisson / negbin).
}}

\item{...}{Currently unused.}
}
\value{
A numeric vector with one element per row of newdata.
}
\description{
Computes the NWQS latent index, the linear predictor, or the response-scale
prediction for new data. The training-sample empirical distribution (saved
as \code{train_components_sorted}) and the globally aligned spline knots
are reused so that newdata is interpreted on the same scale used during
fitting.
}
